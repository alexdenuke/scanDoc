import styles from './Backdrop.module.scss'

interface BackdropProps {
}

const Backdrop = (params: BackdropProps) => {
  return(
    <div>
      <h1>Backdrop</h1>
    </div>
  )
};

export default Backdrop;